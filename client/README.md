# Chorus Client

Flutter client shell for Chorus authentication.

## Run

```powershell
flutter pub get
flutter run -d chrome --dart-define=CHORUS_API_BASE_URL=http://localhost:8000/chorus
```

The default API base URL is `http://localhost:8000/chorus`.
